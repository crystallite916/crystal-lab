Run the crystal-lab test suite.

Execute the following from the repository root:

```bash
source .venv/bin/activate && pytest python/ -v
```

If tests fail, show the full output and identify which project and test is failing. Offer to investigate the root cause.
